language: node_js
node_js:
  - 10

addons:
  hosts:
    - storefront-api
    - api
    - elasticsearch

addons:
  apt:
    packages:
      - libgconf-2-4

services:
  - elasticsearch
  - redis

cache:
  yarn: true
  directories:
    - ~/.cache

git:
  submodules: false

before_install:
  - git submodule update --init

before_script:
  - yarn bundle
  - make start

script:
  - yarn e2e
